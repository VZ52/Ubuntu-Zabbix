# Шаблон переменных окружения. Скопируй в .env и подставь свои значения.
# Сам .env в git не коммитится (он в .gitignore).

POSTGRES_DB=zabbix
POSTGRES_USER=zabbix
POSTGRES_PASSWORD=change_me_to_something_long_and_random
